.venv/
.dataforge/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.uv-cache/
# 本地 Python/uv 与 pytest 临时产物只使用这两个统一根目录。
.uv-task-cache/
.DS_Store
frontend/node_modules/
frontend/.npm-cache/
frontend/package-lock.json
global_llm/
llm_local.yaml
llm_providers.py
build/
tests/
wiki/
# 本地环境变量与密钥（含真实凭据，禁止提交）
# 注意：.gitignore 本身必须纳入版本控制，否则内网资产保护规则无法随仓库共享
compose.yaml
.env
.env.*
secrets/
!secrets/README.md
.env.docker.example
docs\Docker构建优化说明.md
docs\archive\old-plan.md
# ===== 本地专用忽略（不提交到仓库）=====
# 含公司内网资产（具体 IP  与域名），禁止提交
AGENTS.md
specs/006-governed-operator-catalog/analysis.md
scripts/ensure-dataforge-milvus-link.sh
scripts/verify-milvus-egress.sh
scripts/ensure-milvus-egress.sh
llm_servings.yaml
# 本地 IDE / Agent / 包缓存 / 临时目录（不入库）
.idea/
.kilo/
.pnpm-store/
.pytest-tmp/
.devora/
.workbuddy/

# ===== 所有隐藏文件夹（以 . 开头的目录）均不提交 =====
# 防止本地 Agent 配置 / uv 缓存 / 内网相关目录被推送到公开仓库
# 注意：本规则只忽略“目录”，隐藏文件（.gitignore/.dockerignore/.python-version）不受影响
.*/
# 例外：若未来启用 GitHub Actions CI，取消下一行注释以保留 .github/
# !/.github/
specs/
tests/test_v7_platform.py

# 本地 Docker 构建优化说明（内部文档，不提交）
docs/Docker构建优化说明.md
# 本地 LLM serving 配置（含内网地址，不提交）
llm_servings.yaml
# 内部规格文档（含内网地址，不提交）
specs/008-knowledge-collection-lifecycle/
# 前端构建产物（含可能被压缩的内网地址，禁止提交）
frontend/dist/

# 临时迁移/审查用的本地 SQLite（含内网地址，二进制文件，禁止提交）
.tmp-review-migration.db
.tmp-*.db
a.py
