# Absolute path to the DataForge checkout on the Docker host.
DATAFORGE_HOME=/srv/dataforge
# The environment file must contain the DATAFORGE_MILVUS_* and EMBEDDING_* keys.
DATAFORGE_ENV_FILE=/srv/dataforge/.env.docker
