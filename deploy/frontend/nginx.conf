server {
    listen 8080;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # 单文件限制为 200 MB，额外预留 multipart 请求开销。
    client_max_body_size 220m;

    location /api/ {
        proxy_pass http://dataforge-api:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # SSE 任务进度需要关闭代理缓冲。
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    location = /docs {
        proxy_pass http://dataforge-api:8000/docs;
        proxy_set_header Host $host;
    }

    location = /openapi.json {
        proxy_pass http://dataforge-api:8000/openapi.json;
        proxy_set_header Host $host;
    }

    # pdf.js 以 ES 模块方式加载 worker（.mjs）；nginx 默认无 .mjs 映射，
    # 会回退为 application/octet-stream，导致浏览器拒绝作为 JS 模块加载。
    location ~* \.mjs$ {
        default_type application/javascript;
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
