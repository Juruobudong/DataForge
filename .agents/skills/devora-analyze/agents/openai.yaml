interface:
  display_name: "Devora Analyze"
  short_description: "Analyze the active feature for consistency, persist actionable risks, and route each finding to the right owner."
  default_prompt: "Use $devora-analyze for this task."
policy:
  allow_implicit_invocation: true
