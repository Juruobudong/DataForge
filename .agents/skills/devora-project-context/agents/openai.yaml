interface:
  display_name: "Devora Project Context"
  short_description: "Initialize or refresh a durable, evidence-based snapshot of the current project workspace."
  default_prompt: "Use $devora-project-context for this task."
policy:
  allow_implicit_invocation: true
